1. 	Cách1 thực hiện việc lấy dữ liệu và ghép chuỗi HTML ngay tại 
browser.
	Cách2 thực hiện việc lấy dữ liệu và ghép chuỗi HTML tại server 
trước khi gửi file HTML hoàn chỉnh lên server.

2. 
npm install --save-exact ejs@3.1.8

3. 	Template engines dùng để tạo và hiển thị các trang HTML động 
bằng cách kết hợp dữ liệu với mẫu giao diện. Express hỗ trợ rất nhiều 
template engines (EJS, ...).

4.  	Để sử dụng được 1 template engines:
B1: cài đặt bằng terminal: npm --save-exact ejs@3.1.8
B2: khai báo thư mục lưu trữ các template engines, định nghĩa template được sử dụng (app.set('view engine', 'ejs');)

5. 	File .env được sử dụng để lưu trữ các tham số dùng chung. Thay vì 'hardcode', chúng ta sẽ cấu hình tập trung lại.
Nếu lựa chọn giải pháp 'hardcode', mỗi lần user update password, thì dự án có bao nhiêu chỗ dùng 'PASSWORD' sẽ cần cập nhật bấy nhiêu nơi.

6. 	Nodemon dùng để theo dõi các file trong dự án. Khi phát hiện các file thay đổi, nó sẽ tự động restart server => không cần phải chạy lại server khi thay đổi code trong quá trình phát triển web. 
	Mặc định Nodemon sẽ theo dõi các file sau: .js, .mjs, coffee.

7. 	Các thư viện không cần thiết cho production, chỉ cần trong quá trình development thì sẽ được khai báo trong mục "devDependencies". Việc làm này để tránh phải tải những thư viện không cần thiết khi deploy web.

8. 	Cấu hình views engine: đây là cấu hình cho express biết các file giao diện (.ejs) nằm trong thư mục nào.

9. 	Cấu hình static files: việc khai báo để Express biết thư mục nào chứa các file tĩnh (static files) và tự động phục vụ chúng cho client.

10. 	Mô hình MVC 
	View: là kết quả nhìn thấy của client
	Controller: chính là nơi điều hướng dữ liệu 
	Model: kết nối truy cập database để lưu trữ dữ liệu, đọc dữ liệu 

11. 	Design Pattern là cách giải pháp thiết kế để xử lý bài toán một cách hiệu quả nhất.

12. 	Cấu trúc thư mục:
Controllers
Models
Views
Routes: 	Thư mục này sẽ khai báo các rote (đường 		dẫn tới các file views engine).
Public: 	Thư mục chưa các file static (img, css, 			js).
Migration: 	Nơi ghi các cấu hình của database.
Config: 	Thư mục này sẽ lưu trữ các cấu hình config 		cần thiết của dự án express
services: 
Middleware:

13. 	Docker:
Docker tạo ra một môi trường chạy nhất quán (container). Bên trong container, ứng dụng luôn chạy cùng một hệ điều hành, cùng phiên bản Node.js và cùng các thư viện mà bạn đã định nghĩa trong image.
Muốn chạy được Docker cần có một lớp Linux ở phía dưới (WSL2 hoặc máy ảo). Do đó cần phải cài đặt WSL (Windown Subsystem for Linux) thì mới chạy được.

Trong mỗi container cần khai báo môi trường để chạy ứng dụng VD(Linux, Node.js, npm, package, source code, cấu hình, ....)

